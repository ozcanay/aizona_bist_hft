{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Clear",
            "type": "shell",
            "command": "rm -rf build/",
        },
        {
            "label": "Build",
            "type": "shell",
            "command": "cmake -DCMAKE_BUILD_TYPE=Debug --preset=default && cmake --build build --parallel && mv build/compile_commands.json .vscode/",
        }
    ]
}